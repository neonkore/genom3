#!/bin/sh
. ${0%${0##*/}}../test.sh

mkdir -p run.t


# --- component ids --------------------------------------------------------
#
testing '[$component ids]'

cat >run.gen <<EOF
component c;
EOF
run 2 ${GENOM_BIN} interactive -b run.gen <<EOF
puts [[dotgen component] ids]
EOF
