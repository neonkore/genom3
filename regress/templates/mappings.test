#!/bin/sh
. ${0%${0##*/}}../test.sh

mkdir -p run.t


# --- simple invocation ----------------------------------------------------
#
testing 'mappings template invocation'

run 0 ${GENOM_BIN} mappings -C run.t ${thisdir}../component.gen
run 0 ls -R -1 run.t
expect out '
	/^run.t:$/ {next}
	/^c_c_types\.h$/ {next}
	/^$/ {next}
	{exit 2}
'
